name:  Controller workflow entry after receiving pull request event
on:
  pull_request:
    branches: [ "*" ]
    paths:
      - "controllers/"

jobs:
  build-and-push:
    permissions:
      contents: read
      packages: read
    uses: ./.github/workflows/build-controller.yml
    with:
      needs_push: false